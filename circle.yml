test:
    post: 
      - python -c 'import glob; import os; print("\n".join(["cp {} {}".format(coverage_path, os.path.join(os.environ["CIRCLE_ARTIFACTS"], coverage_path.split("/")[1], "coverage")) for coverage_path in glob.glob(".tox/py*/tmp/coverage/")]))' | sh 
